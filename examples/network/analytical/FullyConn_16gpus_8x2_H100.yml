topology: [ FullyConnected, Ring ]
npus_count: [ 8, 2 ]        # 2 nodes, 8 GPUs per node
bandwidth: [ 400.0, 400.0 ] # GB/s per link (NVLink-like; inter-node left same unless you want to reduce)
latency: [ 1 ]

# Dimension 0 (FullyConnected): intra-node NVLink all-to-all
# Dimension 1 (Ring): inter-node links

