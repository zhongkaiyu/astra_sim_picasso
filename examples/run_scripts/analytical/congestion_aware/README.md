# congestion_aware ????????????

## ?�??
- `configs/`??ASTRA-sim ????
- `trace_configs/`??trace ???????�??????
- `bulk_run_configs.py`?????????? trace + ???? ASTRA-sim
- `cache_db/`??`cache_db_test/`?????????CSV/SQLite??
- `csv_to_sqlite.py`??CSV ? SQLite

## ?????????????????????
`<par>_<device>_<physical>_<num_gpus>_<logical>_<model>_<operation>[_b{batch}|_bs{batch_size}|_batch_size{batch_size}][_s{seq}|_sl{seq_length}|_seq_length{seq_length}][_extra].json`

?????`dp16_h100_mesh2d_16gpus_ring_qwen_decode_bypass_b64_s1024.json`
- ?????????`tp/dp/pp/sp/cp` ??? + ??????? `dp16`??
- ?????????`mesh2d/alltoall/...`
- GPU ??????`16gpus` ?? `4x4/8x2` ??
- ????????`ring/direct/...`
- `model` / `operation`?????????????`qwen` / `decode_bypass`??
- ?????`b{batch}`/`bs{batch_size}`/`batch_size{batch_size}`??`s{seq}`/`sl{seq_length}`/`seq_length{seq_length}` ?????� batch/seq

## 1) ???? trace???????
??�????? trace ???�?

```bash
python3 bulk_run_configs.py \
  --gen-trace \
  --trace-configs-dir trace_configs \
  --trace-pattern qwen_decode_bypass_dp16.json
```

trace ???????????`trace_configs/*.json`????
```
{
  "output_dir": ".../et_trace/xxx",
  "output_name": "decode_bypass_dp16",
  "model_type": "qwen_decode_bypass",
  "dp": 16,
  "head": 64,
  "kvhead": 4,
  "num_stacks": 94,
  "dmodel": 4096,
  "batch": 64,
  "seq": 1024,
  "tpsp": true
}
```

## 2) ???? ASTRA-sim????????
```bash
python3 bulk_run_configs.py \
  --configs-dir configs \
  --pattern "*.json"
```

????????????? `cache_db_test/`????????? `--csv/--db` ???????

## 3) CSV ? SQLite
```bash
python3 csv_to_sqlite.py --csv cache_db_test/results_cache.csv --db cache_db_test/results.sqlite
```

