---
description: Chakra Trace 生成规范 (symbolic_tensor_graph_picasso)
globs: symbolic_tensor_graph_picasso/**/*.py
alwaysApply: false
---

# Trace 生成系统

## 入口

`symbolic_tensor_graph_picasso/main.py` — 必须从 `symbolic_tensor_graph_picasso/` 目录执行 (CSV 计算图用相对路径)。

## 两种调用方式

### 方式 1: 通过 bulk_run_configs.py (推荐)
```bash
cd examples/run_scripts/analytical/congestion_aware
python3 bulk_run_configs.py --gen-trace --trace-pattern "*tph4_tps4.json" --pattern "NOMATCH"
```
trace 配置放在 `trace_configs/` 目录。

### 方式 2: 直接调用 main.py
```bash
cd symbolic_tensor_graph_picasso
python3 main.py --model_type qwen_decode_bypass_hmp_fwd --tp_h 4 --tp_s 4 --head_dim 128 \
    --dmodel 4096 --head 64 --kvhead 4 --batch 64 --seq 1024 --num_stacks 94 \
    --output_dir ./et_trace/my_trace --output_name my_trace
```

## 支持的 model_type

| model_type | 用途 |
|-----------|------|
| `qwen_decode_bypass_hmp_fwd` | Ours — HMP Forward (Softmax Deferred) |
| `qwen_decode_bypass_baseline_fwd` | Baseline — HMP Forward (标准 Softmax) |
| `qwen_decode_bypass_tp16` | TP16 标准模式 |
| `qwen_decode_bypass` | 通用 Decode Bypass |
| `qwen_gqa_hmp_fwd` / `qwen_gqa_baseline_fwd` / `qwen_gqa_tp16_fwd` | GQA-only |
| `dense` / `moe` / `moe_ffn` | 基础模型 |

## 符号形状参数 (Symbolic Shape Parameters)

main.py 通过 sympy 符号系统将 CLI 参数映射为 CSV 计算图中的符号变量。

### 模型维度参数

| CLI 参数 | sympy 符号 | 默认值 | 说明 |
|----------|-----------|--------|------|
| `--dmodel` | `Dmodel` | 8192 | 模型隐藏维度 |
| `--head` | `Head` | 64 | 注意力头数 |
| `--kvhead` | `KVHead` | 8 | KV 头数 (GQA) |
| `--head_dim` | `HeadDim` | 0 (自动) | 每头维度，独立符号 |
| `--dff` | `Dff` | 28672 | FFN 中间层维度 |
| `--dvocal` | `Dvocal` | 32000 | 词汇表大小 |
| `--batch` | `Batch` | 64 | 批大小 |
| `--seq` | `Seq` | 1024 | 序列长度 |

### 并行度参数

| CLI 参数 | sympy 符号 | 默认值 | 说明 |
|----------|-----------|--------|------|
| `--dp` | `dp` | 1 | 数据并行度 |
| `--tp` | `tp` | 1 | 张量并行度 |
| `--tp_h` | `tp_h` | 1 | Head 维度张量并行 (HMP) |
| `--tp_s` | `tp_s` | 1 | Sequence 维度张量并行 (HMP) |
| `--pp` | `pp` | 1 | 流水线并行度 |
| `--sp` | `cp` | 1 | 序列/上下文并行度 |
| `--ep` | `ep` | 1 | 专家并行度 |

### HeadDim 独立符号

`HeadDim` 是 CSV 计算图中的独立符号变量，表示每个注意力头的维度大小。

**自动计算**：当 `--head_dim 0` (默认) 时，自动按 `dmodel // head` 计算。

**显式指定**：对于 head_dim ≠ dmodel/head 的模型（如 Qwen），必须显式传入。

| 模型 | dmodel | head | 实际 head_dim | 说明 |
|------|--------|------|--------------|------|
| Qwen-72B | 4096 | 64 | 128 | head_dim = 2 × dmodel/head，必须显式指定 `--head_dim 128` |
| LLaMA-70B | 8192 | 64 | 128 | head_dim = dmodel/head，可省略 (自动=128) |
| GPT-3 | 12288 | 96 | 128 | head_dim = dmodel/head，可省略 (自动=128) |

**CSV 中的使用示例** (以 `gqa_kernel_decode_hmp_fwd.csv` 为例):
```
q 的 shape:  "Batch/dp, 1/1, HeadDim, Head/tp_h"
kt 的 shape: "Batch/dp, 1/1, HeadDim, KVHead/tp_h"
kc 的 shape: "Batch/dp, (Seq-1)/tp_s, HeadDim, KVHead/tp_h"
```

之前使用 `2*Dmodel/Head` 表示 head_dim（Qwen 特有的 2× 因子），现在统一为独立符号 `HeadDim`。

## Trace Config JSON 模板

```json
{
  "output_dir": "{PROJECT_DIR}/symbolic_tensor_graph_picasso/et_trace/{trace_dir}",
  "output_name": "{trace_name}",
  "model_type": "qwen_decode_bypass_hmp_fwd",
  "dp": 1, "tp": 1, "tp_h": 4, "tp_s": 4,
  "kvhead": 4, "head_dim": 128, "head": 64, "num_stacks": 94,
  "dmodel": 4096, "batch": 64, "seq": 1024,
  "tpsp": true
}
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `output_dir` | 是 | 输出目录，支持 `{PROJECT_DIR}` 占位符 |
| `output_name` | 是 | trace 文件名前缀 |
| `model_type` | 是 | 模型类型（见上表） |
| `head_dim` | 否 | 每头维度。省略时自动计算 `dmodel // head`。Qwen 必须显式指定 128 |
| `args` | 否 | 额外 CLI 参数字符串，如 `"--rank_remap block2x2"` |

### 示例: Qwen HMP forward (head_dim=128)

```json
{
  "output_dir": "{PROJECT_DIR}/symbolic_tensor_graph_picasso/et_trace/generated_decode_bypass_hmp_fwd_tph4_tps4",
  "output_name": "decode_bypass_hmp_fwd_tph4_tps4",
  "model_type": "qwen_decode_bypass_hmp_fwd",
  "dp": 1, "tp": 1, "tp_h": 4, "tp_s": 4,
  "kvhead": 4, "head_dim": 128, "head": 64, "num_stacks": 94,
  "dmodel": 4096, "batch": 64, "seq": 1024,
  "tpsp": true
}
```

### 示例: 标准模型 (head_dim 可省略，自动计算)

```json
{
  "output_dir": "{PROJECT_DIR}/symbolic_tensor_graph_picasso/et_trace/generated_dense_tp16",
  "output_name": "dense_tp16",
  "model_type": "dense",
  "dp": 1, "tp": 16,
  "head": 64, "num_stacks": 80,
  "dmodel": 8192, "batch": 64, "seq": 1024,
  "tpsp": true
}
```

此时 head_dim 自动为 8192/64 = 128。

## 输出

生成文件存放在 `et_trace/generated_{trace_name}/`，格式为 `{trace_name}.{rank}.et`。
